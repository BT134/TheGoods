
<div class="columns is-mobile p-2">
    <div class="column is-2">
        <div class="is-size-5 is-txt-weight-bold">
            Categories:
        </div>
        <ul class="">
        {{#each categories as |category|}}
            <li class="p-1">
                <a class="has-text-primary" href="{{category.category_name}}">{{category.category_name}}</a>
            </li>
        {{/each}}
        </ul>
    </div>

    <div class="column">
        <div class="is-size-5 is-txt-weight-bold">
            Articles:
        </div>
        {{#each articles as |article|}}


        {{/each}}
    </div>
</div>



